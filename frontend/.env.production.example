# Production build overrides (never committed).
# Copy to `.env.production` when building locally for production.
# For Vercel deploys, set `VITE_API_BASE_URL` in the Vercel project
# environment variables instead.

VITE_API_BASE_URL=https://event-management-api-aw25.onrender.com
