# Local development overrides (never committed).
# Copy to `.env.local` to target a specific backend while developing.
# Loaded by both `npm run dev` and `npm run build`.

VITE_API_BASE_URL=http://localhost:8000
